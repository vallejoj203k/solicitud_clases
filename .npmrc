# Vite y Tailwind son devDependencies y hacen falta para compilar el frontend.
# Con NODE_ENV=production (lo que usa Railway) npm las omitiria; `include=dev`
# lo fuerza sin importar en que orden se evaluen omit/include.
include=dev
